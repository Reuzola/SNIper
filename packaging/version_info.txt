# UTF-8
#
# PyInstaller version-info resource. Embedded into the EXE so that the
# Windows shell, Explorer's Properties dialog, and — crucially — Defender's
# ML reputation model see real metadata instead of a blank binary. Blank
# version info is a strong signal for the Behavior:*!ml classifier.
VSVersionInfo(
  ffi=FixedFileInfo(
    filevers=(1, 1, 4, 0),
    prodvers=(1, 1, 4, 0),
    mask=0x3f,
    flags=0x0,
    OS=0x40004,
    fileType=0x1,
    subtype=0x0,
    date=(0, 0)
  ),
  kids=[
    StringFileInfo(
      [
        StringTable(
          u'040904B0',
          [StringStruct(u'CompanyName',      u'SNIper'),
           StringStruct(u'FileDescription',  u'SNIper - lightweight SNI-based DPI bypass proxy (user-space, no admin)'),
           StringStruct(u'FileVersion',      u'1.1.4.0'),
           StringStruct(u'InternalName',     u'SNIper'),
           StringStruct(u'LegalCopyright',   u'Released under the MIT License.'),
           StringStruct(u'OriginalFilename', u'SNIper.exe'),
           StringStruct(u'ProductName',      u'SNIper'),
           StringStruct(u'ProductVersion',   u'1.1.4.0')])
      ]
    ),
    VarFileInfo([VarStruct(u'Translation', [1033, 1200])])
  ]
)
